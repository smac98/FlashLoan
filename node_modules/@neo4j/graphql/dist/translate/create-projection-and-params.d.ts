import type { ResolveTree } from "graphql-parse-resolve-info";
import type { Node } from "../classes";
import type { Context } from "../types";
import * as CypherBuilder from "./cypher-builder/CypherBuilder";
export interface ProjectionMeta {
    authValidateStrs?: string[];
    connectionFields?: ResolveTree[];
    cypherSortFields?: {
        alias: string;
        apocStr: string;
    }[];
}
export declare type ProjectionResult = {
    projection: string;
    params: Record<string, any>;
    meta: ProjectionMeta;
    subqueries: Array<CypherBuilder.Clause>;
};
export default function createProjectionAndParams({ resolveTree, node, context, chainStr, varName, literalElements, resolveType, inRelationshipProjection, isRootConnectionField, isInCypher, }: {
    resolveTree: ResolveTree;
    node: Node;
    context: Context;
    chainStr?: string;
    varName: string;
    literalElements?: boolean;
    resolveType?: boolean;
    inRelationshipProjection?: boolean;
    isRootConnectionField?: boolean;
    isInCypher?: boolean;
}): ProjectionResult;
//# sourceMappingURL=create-projection-and-params.d.ts.map