import type { CypherEnvironment } from "../Environment";
import { PropertyRef } from "../expressions/PropertyRef";
import type { CypherCompilable } from "../types";
/** Represents a variable */
export declare class Variable implements CypherCompilable {
    readonly prefix: string;
    id: string | undefined;
    constructor(prefix?: string, id?: string);
    getCypher(env: CypherEnvironment): string;
    property(path: string): PropertyRef;
}
/** For compatibility reasons, represents a plain string variable */
export declare class NamedVariable extends Variable {
    constructor(name: string);
}
//# sourceMappingURL=Variable.d.ts.map